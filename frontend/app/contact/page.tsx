import ContactExperience from "@/components/contact/ContactExperience";

/* /contact — Custom Design page.
   A single-purpose page for visitors who want a custom theme designed
   for their brand. No theme catalogue needed — just the form. */
export default function ContactPage() {
  return (
    <main>
      <ContactExperience />
    </main>
  );
}
